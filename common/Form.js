import styles from '../styles/Home.module.css'

export default function Form({...name}) {
    return (
    <div className={name}>
        <modal id="addForm" hidden>
            
        </modal>
    </div>
  )
}
